{
  "name": "cachifyjs",
  "version": "1.0.5",
  "type": "module",
  "description": "CachifyJS is a  js package to cache api responses and call them automatically and repeatedly. That can be controlled by parameters.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mohammad Ali",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.3.5"
  }
}
