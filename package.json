{
  "name": "hardcache",
  "version": "1.0.0",
  "type": "module",
  "description": "hard cache is a  js package to cache api responses and call them automaticly and repeatedly. That can be controller by parameters.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mohammad Ali",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.3.5"
  }
}
